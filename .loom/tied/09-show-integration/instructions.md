# 09-show-integration

Presets reach the Show tab: portable references, expansion at send,
capture-as-step. **Needs 06 tied.** Split into two ordered children per the
first review's C2 seam — the reference foundation is prerequisite
architecture, not preset code:

1. `1-reference-foundation` — the Show model learns references and portable
   named targets.
2. `2-preset-messages` — the `/preset/*` family rides on it.

Work them in order; this parent ties when both are tied. Shared authority:
proposal §6, addendum §9-Q3 and §10.7, first review D8/C2,
`.loom/tied/3-addendum-review/review-2.md` confirmed-closures for D8.

**COMPLETED 2026-07-29.** Both ordered children are tied. Shows now carry
portable content references and named-group targets, apply patch presets
through the shared application core, flatten preset references to ordinary
parameter messages, and capture the current target-to-preset arrangement as
one undoable step. The two pre-existing browser-suite failures found during
the child work are isolated in `11-browser-test-failures`.
