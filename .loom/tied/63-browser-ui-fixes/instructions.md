# Browser interaction and device-status polish

Resolve two browser-facing dashboard defects reported from live use: make the
modulation drawer's numeric fields reliably editable with a primary click, and
improve the Device tab's status/action hierarchy and RSSI legibility.

The two child stitches are independent implementation slices and each retains
its focused browser verification.
