# Resolution (2026-07-10)

This gate is satisfied — Bob chose to run S1 as a **council session, broadened
to the whole bopOS↔patch responsibility seam**. The council ran the same day:
`.loom/tied/seam-0-council/` (ground truth, five expert designs, judgment,
Bob's ratification). Every checkbox in this stitch resolved there:

- Session format: council (five opus experts, judge inline on fable).
- Draft §8 questions settled: frame-form wire (B) + sparse authoring form,
  falloff enum ints, routed single receiver with flat args (Bob's 7.2 ruling),
  no manifest declaration of points in v1, raw distance deferred.
- Output: ruling + ratification; the contract amendment is drafted in
  `patch-seam/seam-1-contract-amendment` (`.waiting` on Bob's text review).
- Implementation stitches spawned: `seam-2` (master), `seam-3` (points —
  the implementation this gate guarded), `seam-4` (facilitator promotion),
  `seam-5` (SC starter kit). `spatial-1` re-scoped; `spatial-2` unchanged.

S2 (multi-element) was largely dissolved by Bob's ruling R2: one engine
instance, the patch clones N elements internally; assignment carries a true
N-position list. What remains of S2 is the seam-1 §5 text plus, someday,
per-element liveness — deferred with evidence named in the ratification.
