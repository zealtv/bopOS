# 2-remote-derived-grid

Describe the intention here.
# Remote derived grid

Converge Remote onto the shared downward card grid after `1-control-card-set`.
Derive All, every group, and every Seat in common order; render labels rather
than target pickers; remove presets; keep dashboard-only declarations and
Remote device commands. Split bundled host capabilities into named behavior.
Update focused Remote tests. Do not implement identity borders here.
