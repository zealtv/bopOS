# Thread verification

The final implementation pass completed 281 browser-free tests. The 24 browser
journeys were attempted but all were blocked before navigation because the
managed macOS sandbox hides Chromium's required `SystemAppearance` bundle.
Focused browser journeys were updated and compile, but no rendered pass is
claimed. No iPad, hardware, audio, or Pure Data verification was performed.

See each tied child's verification record for its exact commands and scope.
