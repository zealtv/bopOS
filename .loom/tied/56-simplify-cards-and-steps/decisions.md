# Simplify cards and steps — completion

The thread's five ruled outcomes are shipped:

- Capture as step is retired; steps are hand-authored.
- Step playback applies presets before explicit parameter modifications.
- Preset dirty provenance distinguishes its causes.
- Cards target one thing, order themselves All → groups → Seats, and overflow
  downward in a shared Control/Remote grid.
- Control authors its card set; Remote derives every target without picker or
  presets; both share Seats-map identity strokes.

The card design amendments Bob supplied during review are authoritative in
`4-cards-design.tied/decisions.md`.
