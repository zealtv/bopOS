# param-automation-fresh-context

Prepare bopOS for a fresh-context continuation at
`16-param-automation/automation-2-show-builder-gui` without claiming that
implementation stitch:

- audit the live Loom ordering and ratified automation authority;
- correct stale parent-thread wording left from before automation-0/1 tied;
- leave the newest `.notes/handoff-*.md` with exact starting files, settled
  grammar/constraints, verification baselines, known local-state guardrails,
  and the automation-2 → automation-3 → automation-4 gate sequence;
- verify referenced paths, commands, contract terms, and clean worktree scope;
- tie, commit, and pause.

Never edit `.pd` files and never stage Bob's untracked `dashboard/shows/`.
