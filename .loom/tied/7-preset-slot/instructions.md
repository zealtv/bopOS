# 7-preset-slot

Fifth slice of the ratified control-panel design (authority: the tied
`2-control-panel-design` stitch).

The provisional preset row at the panel top: patch name (live) +
preset dropdown + `new`/`save`/`del` (momentary), rendered **disabled** —
the slot exists so `41-preset-primitive` lands into a designed home rather
than redesigning the panel. No preset behavior in this stitch; 41 remains
gated behind `44-event-plane` per the 2026-07-27 queue order.

Verify: render assertion in the nearest living journey; disabled controls
carry an explanatory title/aria-description pointing at the preset system
being future work.
