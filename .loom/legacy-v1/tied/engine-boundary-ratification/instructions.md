# engine-boundary-ratification

Bob's decision gate for the engine-boundary council ruling.

After `engine-boundary-council` is tied, read its `judgment.md`, rejected
alternatives, verified constraints, and smallest first slice with Bob. Record:

- accepted/rejected/amended ruling;
- exact contract and naming decisions;
- compatibility or rewrite policy;
- which existing tied behavior is superseded;
- ordered implementation stitches; and
- any decisions deliberately left open.

If Bob has not ratified, mark this stitch `.waiting` with the unresolved crux.
Do not implement beyond this gate.
