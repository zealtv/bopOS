# boundary-2-pd-parallel-relay

Prove PD consumes the same selector-stripped localhost surface as every engine.

- Remove the PD exclusion from master and `/p/*` relay delivery on 6661.
- Retain PD's direct 6660 path in parallel as a reversible safety net.
- Verify master, parameters, identity, points, cues, and notifications reach PD
  through the common surface using the audition rig and simfleet as applicable.
- Record duplicate-delivery effects and resolve them without weakening the
  ratified end state.
- Do not add a `/helper/*` alias; the ratified policy is a clean break.
- Never edit `.pd` files. Stop and surface evidence if the common surface is
  not ready for the later PD edit wave.
