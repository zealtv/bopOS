# parameter-addresses

Allow a patch to expose clean, arbitrarily nested parameter addresses such as
`/p/instrument/marimba/gain` and `/p/fx/reverb/mix`, consistently across the
manifest, dashboard, persistence, fleet wire and engine surface.

Start with the waiting design/contract stitch. Do not implement or amend the
ratified OSC contract without Bob's ratification.
