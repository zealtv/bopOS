# friction-0a-readme-refresh

Audit the root README against the implemented July 2026 system and rewrite it
as the accurate entry point for operators, patch authors, and contributors.

- Replace the removed `DASHBOARD.pd`-centred workflow with the web dashboard,
  simulator, and audible rig.
- Describe persisted dashboard assignment and `bopos.devices` as an optional
  seed, not an identity gate.
- Make the v1.2 sole-LAN-listener engine boundary and six-port topology clear.
- Document manifest-declared engine/entrypoint/params, provided terms, PD and SC
  adapters, run context, and current patch-management path.
- Refresh Pi/laptop setup, repository layout, and links to durable docs.
- State remaining hardware/Linux gates honestly; do not turn plans into shipped
  features.

Verify every referenced path and command against the repository, check Markdown
links, and retain a short results record before tying.
