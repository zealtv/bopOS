# input-1-hw-recipe

**.waiting — needs an input-capable HAT + Bob.**

- [ ] Verify capture end-to-end on at least one input-capable HAT (HiFiBerry
      DAC+ ADC, IQaudio Codec Zero, or USB interface); document the recipe.
- [ ] Hardware guidance doc: which HATs give line/mic in vs the out-only
      DigiAmp+; per-project choice, bopOS documents the tested ones.

Opens when input-0 is tied and hardware is on the bench.
