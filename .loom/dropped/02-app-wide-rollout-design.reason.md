# Dropped 2026-07-30 — superseded by `02-component-unification`

This stitch assumed a top-down order: audit the app, ratify a token/pattern
system, then roll it out. Bob reversed that on 2026-07-30:

> "A good process here might be to do a pass to identify the reusable
> components and then design them one at a time and then integrate them into
> the application and then out of those components extract a coherent design
> language."

Components first, language extracted from shipped components. The stitch's
intent is not lost — its audit deliverable was produced as
`.notes/component-inventory-2026-07.md`, and the token/pattern work survives
as the concrete `02-component-unification/02-token-promotion` child rather
than as a speculative system designed ahead of its second consumer.

Nothing here was worked; no artifacts are stranded.
