# dashboard-6-rig-adoption

**.waiting — this is the dashboard goal's tie gate, and it's Bob's.**

The web dashboard becomes the daily driver on a real installation and
DASHBOARD.pd is retired. Outstanding real-rig checks (from the goal stitch):
every tied stitch's hardware pass, the PD edits list
(`.notes/pd-edits-for-bob.md`), and an iPad touch pass on `/facilitator`.

(This blocker child replaces the `.waiting` suffix that previously sat on the
`dashboard` goal itself — waiting belongs on loose ends, per `.loom/README.md`.)
