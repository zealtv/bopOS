# fleet-testing

**Goal:** real-fleet acceptance for the things software tests can't prove —
asset rollout, clock sync, spatial audio.

**Status:** all software is built and tied. What remains is hardware evidence,
plus one ready data task (the Northern Broadwalk site plan).

| child | what's left | status |
|---|---|---|
| `asset-fleet-distribution` | design + prove multi-node asset rollout | parked by Bob |
| `clock-sync` | measure event spread on real installation Wi-Fi | needs rig |
| `spatial-audio` | audible multi-node sweep; Northern Broadwalk venue | site plan ready; sweep needs rig |

Closes when asset rollout is accepted and clock + spatial have evidence from a
representative physical fleet. Each child records rig, network, node count,
patch/engine, commands, measurements, what was heard, and limitations.
