# 17-automation-polish

Bob's 2026-07-20 feedback pass on the freshly tied `16-param-automation`
work: two visual bugs in the Show-tab fade builder, a rework of how
automated parameters show on live sliders, and a question about the
per-step "loop forever" control.

Source: Bob's feedback message, 2026-07-20 morning (AEST). Screenshots
referenced by the child stitches live in
`.loom/threads/dashboard-theme-toggle/` (they were dropped there; the two
inspector screenshots belong to this thread's ap-1/ap-2).

Order: ap-1 → ap-2 → ap-3 → ap-4 (small bugs first; ap-3 carries the one
design-review step; ap-4 is quick analysis + likely removal).

Rulings for this thread (Bob, 2026-07-20, via AskUserQuestion):

- ap-3's design step is a **single Opus design pass** (not a full
  council); screenshots for Bob's veto, recorded as an amendment note
  against the automation-4 judgment.
- ap-4: if no reason to keep "loop forever" is found, **remove it**
  (existing show files must still load), and notify Bob of the reasoning.
