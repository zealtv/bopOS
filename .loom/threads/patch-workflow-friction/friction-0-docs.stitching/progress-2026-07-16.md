# Progress — documentation review, first full pass (2026-07-16)

Status: **claimed, deliberately not tied.** This session produced the
complete restructured documentation set as a draft for Bob's review. The
stitch stays `.stitching` until the remaining items below are closed.

## What landed

New documentation architecture, three routes from the README front door:

| document | role |
|---|---|
| `README.md` | rewritten as the friendly front door: what/why/how-it-fits, three routes, documentation map, honest status. All install commands and the port table moved out. |
| `docs/GETTING-STARTED.md` | new — 10-minute simulated-fleet tour with real screenshots and a hands-on exercise. |
| `docs/COMPOSING.md` | new — the composer guide (Bob's 2026-07-13 first-class deliverable): folder patch, manifest, `[bopos]` buses, Simulation audition, Deploy as fleet patch, assets, perform. **Advanced workflow with Git** is its own closing section; Git appears nowhere else. |
| `docs/INSTALL.md` | new — Pi node install (moved from README), the travel-router network checklist, provisioning boundary, unattended-update rules. |
| `docs/ARCHITECTURE.md` | new — non-normative system tour with mermaid diagrams: fleet shape, node internals, Seats/devices/elements/groups, planes, sync, spatial, distribution, safety. Defers to the contract throughout. |
| `docs/PORTS.md` | new — one-page port map + transport rules quick reference; contract §4 named as normative. |
| `docs/OSC-CONTRACT.md` | presentation-only: the amendment provenance blob moved verbatim into a new §15 Revision history table; a short reader's guide added up top. **No normative text changed.** |
| `dashboard/README.md` | stale labels fixed against the live UI ("Set fleet patch" → "Deploy as fleet patch"; "Start simulation" → the Execution target toggle); guide links added. |
| `patches/README.md` | stale per-device "send patch to selected devices" flow corrected to the fleet-deploy reality; linked to COMPOSING. |
| `assets/README.md` | Assets-tab screenshot added. |
| `docs/images/` | six real screenshots captured from `dashboard/server.py` + `tools/simfleet.py` (seeded 4-seat + 1 unassigned fleet) via headless Chromium: all five landed tabs plus the Dashboard per-Seat live view. `shoot_docs.py` in this directory reproduces them. |

## Verification run

- `shoot_docs.py` (this directory): real server + simfleet on ports
  18310/15310/16310, six captures, all inspected by eye.
- `check_links.py` (this directory): 14 documents, **0 broken relative
  links/images**.
- Stale-term sweep (Set fleet patch / old contract versions / getsamples /
  gdrive) across all guides: clean.
- UI labels in the guides were walked against the live simfleet dashboard
  this session: tab names, header controls (Execution target, MUTE ALL),
  Patches tab (Deploy as fleet patch / Revert / Refresh catalog / New
  patch… / Launch Editor / Save manifest), Devices tab actions, Seats tab
  controls (Create Seat / Add Point / venue Load / Save as…), Assets tab.

## Remaining before tie

1. **Bob's review.** The README voice, the doc architecture, and COMPOSING
   are user-facing; treat the whole set as a proposal.
2. **SSID convention (resolved 2026-07-16).** INSTALL.md deliberately
   names no example SSID — Bob's real SSID must never appear in the repo,
   and the recipe instead tells operators to pick their own standing
   SSID/password pair, keep it identical across rigs, and record it in the
   venue kit.
3. **Audible walk.** The Simulation/audition copy in COMPOSING and
   GETTING-STARTED was written from `dashboard/static/js/dashboard.js`
   confirm strings and dashboard/README, not an audible run (no PD in this
   container). One real Simulation-mode walk (ideally macOS) should confirm
   the flow reads true, then relabel if needed.
4. **Zip-upload decision gate (assessment, not built):** the parent
   thread's gate asks whether the git path is too steep for workshops. The
   ratified host-mirror Deploy flow already gives composers a fully git-free
   path, so **no zip-upload proposal appears warranted**; recording that
   judgement for Bob to confirm closes the gate.
5. `/facilitator` standalone view was not screenshotted or label-walked
   (only the embedded Dashboard tab was); one pass if Bob wants it
   documented beyond the current mentions.
6. Real-hardware claims (Pi install steps, DigiAMP bench facts, update
   flow) are labelled honestly but were not re-run this session.

`friction-1-starter-kit` stays waiting behind this review so demo copy can
follow the final information architecture.
