# 06-map-tab

**Deferred by Bob, deliberately.** He named it so the dock would be designed with
room for it, and said explicitly: "we don't need to implement it right now."

> One of the tabs that we're going to want there down the line... will be a map
> view showing a view of the seats and showing the position of points and other
> spatial or upcoming spatial automations that we might implement. Essentially,
> like a map overview monitor that lets us see a state of the composition from
> above.

## Waiting on

- `01-dock-design` ratified and the dock actually shipped.
- Enough of the spatial automation picture to know what there is to monitor —
  point motion and decomposed curves are deferred to the Bob-gated
  `scene-sequencing` co-design, and this view is most of its value once those
  exist. Coordinate with `spatial-audio` and `scene-sequencing` rather than
  duplicating `dashboard/static/js/spatial.js`.

Do not claim this ahead of Bob asking for it.
