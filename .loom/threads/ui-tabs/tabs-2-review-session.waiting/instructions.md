# tabs-2-review-session

The interactive UI review with Bob, held **inside the real tabbed dashboard**
(tabs-1 tied) so feedback is concrete. This is the deliberate back-and-forth
checkpoint Bob asked for on 2026-07-14 — a live session, not a solo pass.

Run the dashboard against simfleet (and managed audition if audible preview
helps), walk each tab with Bob, capture rulings in a session record
(lore-kept), and split follow-up fix stitches under `ui-tabs` as needed.

Bob-gated by nature: stays `.waiting` until tabs-1 is tied and Bob is
available.
