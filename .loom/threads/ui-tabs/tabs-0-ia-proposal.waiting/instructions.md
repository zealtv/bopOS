# tabs-0-ia-proposal

Write the information-architecture proposal for the tabbed dashboard: which
controls and panels live in which tab (Overview / Spatial / Fleet management /
Patch editor), what stays global (master, Silence All, connection status?),
how the facilitator/technical switch is presented consistently, and what — if
anything — device detail retains when fleet management centralises the verbs.

Inputs:

- The ratified fp-0 fleet-patch design (this stitch is `.waiting` until that
  lands — its conclusions define Overview vs Fleet management content).
- `dashboard-9-ui-review` and `ui-0..3` tied records for the current layout's
  known issues.
- The seats model (`.loom/tied/dashboard-8-identity-sim-design/`).

Categorise per Bob's framing: what needs *visibility* (overview) vs what is an
*operation* (fleet management) vs what is *authoring* (spatial, patch editor).

Deliverable: a short written tab map (lore-kept). Mark `.waiting` for Bob's
ratification. Keep it cheap — the deep back-and-forth happens in tabs-2 inside
the real tabs, not on paper.
