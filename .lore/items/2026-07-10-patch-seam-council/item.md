# Council: the bopOS↔patch responsibility seam — designs, judgment, ratification

The council-design-session of record for the seam law. Instigated by the
reverted `spatial-0` engine; Bob broadened the planned S1 spatial session into
a full seam review. Five independent experts (framework boundary, protocol
design, patch author, ops reality, simplicity guard — all on opus) designed
from verified ground truth; the judge (fable, inline) verified the
load-bearing claims (notably: no rig runs the dashboard master yet; the
contract's §1/§4 contradicted its own §3/§6) and ruled the seam law: **bopOS
provides named full-state terms the patch enacts, owns the machinery that
produces them, enforces only mute, and never composes a provided term into a
patch parameter.** Bob ratified same day with amendments: backwards
compatibility disregarded (patches rewrite in lockstep — master ships
unconditionally, no `subscribes` gate); multi-element = one engine instance
cloning N positioned elements internally, true-N position list (pos2
retired); SuperCollider first-class in the starter kit (agent-buildable);
routed single receiver with flat args; facilitator promotion split by owner
(manifest flag for params, install-level default-empty allowlist for verbs);
PD edits surfaced in a top-level waiting-room stitch.

## Source

Loom stitch `patch-seam/seam-0-council` (tied 2026-07-10), run with the
ai-kit `council-design-session` skill in one session, Bob interactive for
frame confirmation, ratification, and follow-up rulings.

## Outcome

Ratified with amendments (`content/ratification.md` wins over
`content/judgment.md` where they differ). Implementation staged behind the
`patch-seam/seam-1-contract-amendment` gate (amendment draft awaiting Bob's
text review), then `seam-2` master / `seam-3` points / `seam-4` facilitator
promotion / `seam-5` SC starter kit. `spatial-audio`'s
`spatial-0b-redesign-review` gate closed into this ruling.

## Tags

- council
- seam
- osc
- spatial
- facilitator
- multi-element
- design
