# Handoff — 2026-07-08 autopilot session

## State of play

The OSC contract implementation run is essentially finished and the
dashboard era has begun: patch-manifest (Crux 2) is complete, the web
dashboard exists and replaces DASHBOARD.pd's core function (phase 1,
contract-native from birth), and `/os/fetch` with the assets landing
convention is live end-to-end. Every `osc-schema-contract` child is tied
except `os-admin-verbs` — a gap discovered while tying up: the §7 admin-verb
rename was nominally mapped to hb-identity but never scoped into any child,
so it's now its own small stitch. The whole run was executed with Codex
doing bounded builds from written specs and this session doing design,
review, hardening, and live verification — that split worked well (three
stitches in one session) and is worth repeating.

## Tied this session (all local commits, awaiting Bob's push — ahead 3)

- **patch-manifest** — `2694126`. Resumed from the 30% pause point;
  helper serves `/os/params` (verbatim) + `/os/report` (11 declared facts),
  launcher is manifest-driven with engine.pid/engine.name, simfleet routes
  `/p/*`. The `route p` PD spec + §13 bare-alias revision proposal went
  into `pd-edits-for-bob.md` §4.
- **dashboard-1-core** — `e160cc4`. `dashboard/` (FastAPI + vanilla JS):
  uid-keyed device list from v1 heartbeats, params rendered from the
  manifest declaration (UNDECLARED badge fallback), master mute, report
  panel, wire-compat matrix in one place (`osc_bridge.LEGACY_COMPAT`).
  Design deltas vs the pre-contract design doc are recorded in the tied
  stitch. Browser-verified headless (screenshots in `.loom/tied/
  dashboard-1-core/`); the browser pass caught and fixed a lost-final-
  slider-value bug.
- **fetch-landing** — `6e1db3b`. `python/fetcher.py` (manifest diff,
  Range resume, prune-to-converge), helper worker with request coalescing,
  legacy samplepacks adopt+symlink, `ASSETS`/`BOPOS_ASSETS` handoff,
  dashboard serves any slot air-gapped with generated manifests.

## In flight / waiting

Nothing claimed. New loose end: **`osc-schema-contract/os-admin-verbs`**
(split this session, instructions written) — helper answers `/os/reboot`
etc. on 6660 with `/os/rev` convergence replies, then the dashboard flips
its admin row off legacy and the parent goal stitch can tie.

## Recommended next

1. `os-admin-verbs` — small, well-specified, finishes the contract thread.
2. `dashboard-2-spatial-facilitator` — but the facilitator view is a
   **decision gate** (user-facing): write the design proposal, mark
   `.waiting`, surface to Bob before building UI.
3. `sample-distribution` can now build its fleet-sync UI directly on
   `/os/fetch` + the dashboard's asset serving.

## Awaiting Bob

- `git push` (3 commits ahead).
- PD edits: `pd-edits-for-bob.md` §1 (restart-engine route), §2 (identify
  chirp), §3 (retire PD hb/aloha), **§4 (route p — dashboard params reach
  patches through PD only after this)** + the §13 revision to ratify.
  Nothing blocks on these (aliases/legacy spellings still work).
- Real-rig verification for all three stitches (each tied stitch's
  verification.md says exactly what loopback did and didn't cover; the
  dashboard also wants a real-WiFi broadcast + DASHBOARD.pd-coexistence
  check).

## Gotchas (healed where possible)

- Codex's sandbox can't open sockets or launch Chromium → have it write
  tests/scripts, run them yourself (healed into both codex skill
  Learnings). Codex was healthy all session; gpt-5.5/5.6 aren't
  addressable by name on this account — the CLI default (5.5-line) works.
- Stitch tests must find the repo by marker, not `../..` depth — tie moves
  the directory (healed: CLAUDE.md note + the tied dashboard test fixed).
- `pkill -f` self-match kills the Bash tool's own shell, exit 144 (healed:
  memory `shell-pkill-self-match`; use `[b]racket` patterns).
- This box has pip 24.0 — the "laptop has no pip" note in older stitches
  was about a different machine. Dashboard dev venv (fastapi/uvicorn/
  python-osc/websockets/playwright+chromium) is in the session scratchpad;
  rebuild via `dashboard/requirements.txt` + `pip install playwright &&
  playwright install chromium`.

## Usage at stop

5-hour session cap at **79%** (threshold 85), resets 2026-07-07T20:40Z
(~4 h from session end). Weekly Fable 61% (resets 2026-07-13), weekly
all-models 37%. A fresh session after the reset has full runway;
`os-admin-verbs` fits comfortably.
