# Bob's braindump — 2026-07-21 (verbatim)

Okay, these are some fixes to get into the show tab and some other places. Let's get this organized and laid out on the loom.

I'd like the terminals to collapse and expand together, so they're essentially one window, the output and the input OSC terminals are a single window or frame. That might in fact become a kind of console down the bottom, a little bit like VS Code with different tabs for incoming OSC, outgoing OSC, and maybe some sort of terminal that lets us send OSC messages by typing them As well as perhaps some other like system utility stuff that could be a tab down there in a kind of like system overview inspector kind of thing. I'd leave the name up to you.  In a narrow view, each of those views could be a tab on a single panel, but in a wider view, we should be able to drag those tabs either side so that they snap to the sides of that wider panel.  Think console tabs in VS Code. I think one of the tabs that we're going to want there down the line, we don't need to implement it right now, but it will be a map view showing a view of the seats and showing the position of points and other spatial or upcoming spatial automations that we might implement.  Essentially, like a map overview monitor that lets us see a state of the composition from above.

When the inspector is expanded In a narrow view, its little like closing button is overlapping the delete button in the tool bar.

Also when in the toolbar, when it's narrow, the divider button is a minus and the step button is a plus, and that is confusing iconography because it seems like it's add step and remove step. I think we need better icons for step and divider.

As far as the dividers go in the step list, I'd like the lines either side of the divider title to not extend the whole way across the divider.  Refer to the screenshot of the prototype Chrome that I was referring to in the previous revision, if you can. But if you can't, it was that there were just two short lines either side of the divider title.  and dividers that don't have a title, I'd like to get rid of that gradient. We don't need it anymore now that we have the grab handle. it can just be a blank line.

In the message inspector, when the parameter is set to an LFO, the number box defining the duration of the period is obscured by the increment and decrement buttons. Space could be made for that by shrinking the unit box.

The green tint that is throughout the website on the light theme, for example on the named steps, I'd like to shift that a bit towards cyan, so we're really it's more purple in cyan than it is purple and green.

And then on the seats page, the listener widget, I want us to find a way to be able to expand and contract the listener range that we currently drag that widget out, that little sort of knobbly line that we drag out, I'd like that interaction to be constrained to the listener dot itself. The issue is that if the listener is up, for example, to the top of the map, I can't extend its listening distance because it clips the map. So I think we need some sort of interaction on the listener that lets us dial in and out the listener range and perhaps indicate it in another way other than the line. Some sort of gradient with a thin line around the outside, or some sort of just outline that shows the maximum range of the listener that we can control with some sort of interaction at that listener point is what I'm aiming for.  That's a bit of a design challenge, so that is one to put to a UX UI expert to ideate on and come up with a good solution.
