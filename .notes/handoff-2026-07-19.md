# Autopilot handoff — 2026-07-19 (evening session)

## State of play

The 15-show-polish sweep is through p3 and the param-automation thread is
further along than the plan required: the OSC contract is at **v1.8**
(§3.2 parameter-automation grammar), and the node-side generator engine
plus simfleet parity are **built and tied** (`python/paramgen.py`). Show
operators now get: values that can be 0, honest rapid transport clicks,
exclusive one-step playback with progress fill and armed pulse, a global
transport strip, all-cues-forward-sync with a persisted settable cue
lead, and (in review, see p4) a resizable step-list scroll box. All work
this session was implemented by codex delegates against spec files
written into each stitch dir, then diff-reviewed, taste-passed, and
re-verified by the orchestrator before tie.

## Tied this session (with commits)

- `automation-0-design-ratification` — contract v1.8 §3.2 + thread
  decomposition (`1e7082e`).
- `15-show-polish/p1-zero-value-and-transport-bugs` (`69753f8`).
- `15-show-polish/p2-exclusive-playback-and-progress` (`c4d123b`) — also
  fixed a real adjacent bug: the 500 ms countdown re-render wiped
  uncommitted saved-shows dropdown picks during playback.
- `16-param-automation/automation-1-engine-and-parity` (`9a9e875`) —
  includes an mtime-keyed manifest-declaration cache in bopos.py added
  in review (Zero 2 W slider-drag rates), and an amendment to the tied
  `boundary-3-framework-slimdown` verify, which had been failing since
  the v1.7 `/admin` amendment (predates this work; confirmed on HEAD).
- `15-show-polish/p3-global-transport-and-cue-lead` (`520b9cf`).

## In flight

- **`p4-step-list-scrollbox` is claimed** and a codex run against
  `.loom/threads/15-show-polish/p4-step-list-scrollbox.stitching/codex-spec.md`
  was launched at session end. If its working-tree changes are present
  but unreviewed: read `codex-report.md` in the stitch dir, `git diff`
  (show.js + style.css only expected), run the acceptance list at the
  bottom of the codex-spec yourself, taste-pass the CSS (codex once
  delivered an `!important` override block instead of editing the
  one-line blocks in place — that idiom check matters), then worklog,
  `./.loom/loom.sh tie p4-step-list-scrollbox`, commit. If the diff is
  absent or wrong, release the claim or redo — the spec is complete.

## Next

`p5-inspector-defaults` → `p6-drag-and-keyboard-editing` (split if it
fights back) → `p7-patch-tab-tidy` → `p8-docs-and-handoff`, then
`automation-2-show-builder-gui`. The delegation pattern that worked:
write `codex-spec.md` into the claimed stitch dir (design decisions
settled first — codex executes, never designs), launch
`codex exec -s workspace-write` in background, review + re-verify
yourself, tie, commit. Tell codex explicitly not to touch `.loom` state
and to end with a report file.

## Awaiting Bob

- `automation-4-waveform-ux-gate` — UX proposal before any waveform
  visualisation code (thread-16 instructions mandate `.waiting` at the
  gate + push notification).
- Everything long-standing in `.waiting` (hardware gates, scene-sequencing
  co-design) is unchanged.

## Gotchas (healed where marked)

- Tied verifies can be stale-red on HEAD (boundary-3 was, since
  2026-07-17): before blaming your change, stash it and re-run. HEALED
  for boundary-3.
- The 500 ms countdown re-render eats uncommitted form state anywhere in
  `#show-root`; `render()` now carries the shows dropdown and cue-lead
  input across rebuilds — new form controls in the Show tab must follow
  the same pattern (p4's scroll box height/position does).
- `claude-usage` reads the macOS Keychain now (HEALED in ai-kit); codex
  sandbox can't bind loopback ports, so its verify claims need
  orchestrator re-runs (in codex-implement Learnings).
- `dashboard/shows/test.json` is Bob's untracked local test show — don't
  `git add -A dashboard` (it briefly got committed and was amended out).

## Usage at stop (2026-07-19 ~21:30 local)

- 5-hour session: ~78% before the p4 review, resets 2026-07-19T14:10Z
  (next 5-hour window).
- Weekly Fable: 72%, resets 2026-07-20T09:00Z (tomorrow morning UTC).
- Weekly all-models: 50%, same reset.
- Codex weekly: 10% used, resets 2026-07-25 — the delegation budget is
  effectively unconstrained (Bob authorized down to ~20% remaining).
