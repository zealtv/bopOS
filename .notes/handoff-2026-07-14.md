# Handoff — distribution contract + node side tied (2026-07-14)

State at handoff: OSC contract v1.3 and the node/framework half of the ratified
patch + asset mirror are tied. Nodes can safely converge host-mirrored patches,
list/drop patches and assets, report fetch phases and hostname, require valid
manifests, and use the explicit `updatebopos` spelling; simfleet matches. The
legacy gdrive/getsamples and manifest-less launch paths are gone. No stitch is
claimed. Bob requested a pause before `dist-3-dashboard-send`, which is next.

Important temporary seam: the node now speaks the v1.3 hard break, while the
dashboard still has its old Update/Get samples/typed-switch surface. Do not
treat this midpoint as a deployable complete composer workflow; dist-3 updates
the dashboard onto the tied verbs and Send/Sync model.

## Stitches tied this session

- `dist-1-contract-amendment`: implementation `41d7178`, tie `1e78500`
  - contract v1.3, distribution/migration text, hostname report field
  - rewritten patch workflow docs and corrected top-level manifest claims
- `dist-2-node-side`: implementation `2898ffd`, tie `d85ad5b`
  - atomic staged patch convergence with mandatory manifest validation
  - active-patch stop/converge/restart, Git-XOR-mirror enforcement
  - patches/drop/updatebopos/progress/report verbs and simfleet parity
  - focused verifier and evidence in `.loom/tied/dist-2-node-side/`

## In-flight / waiting

No in-flight stitch and no dangling `.stitching` directory. Existing waiting
hardware/decision stitches remain unchanged. `friction-0-docs` still waits for
both dist-2 and dist-3; `friction-1-starter-kit` still waits for dist-4.

## Recommended next stitch

Claim `dist-3-dashboard-send`. It is the required other half of the v1.3 hard
break: serve patches, implement per-item Send plus Sync all, consume fetch
progress and `/os/patches`, add drop/dropdown/Git visibility, confirm active
patch sends, and replace Update/Get samples UI and wire spellings. Then take
`dist-4-demos` before the dashboard seats thread.

## Decisions awaiting Bob

None. The run stopped at Bob's explicit pre-dist-3 pause, not a design gate.

The one-release `patches/<active>/bop/samplepacks` compatibility symlink remains.
Bob's hard-break ruling retired gdrive, getsamples, patch-level config, update
naming, and undeclared launch, but did not retire this patch-facing path. Fetch
safely omits the exact expected link during staging and rejects every other
destination symlink; the launcher recreates it for the active patch.

## Gotchas and verification

- `.loom/tied/dist-2-node-side/verify_dist2_node_side.py` passes completely.
  It covers real temp-directory convergence/pruning, atomic invalid-manifest
  preservation, malicious and allowed symlink cases, Git refusal, active
  lifecycle success/failures, coalescing, new verbs/replies, and serialized
  simfleet parity.
- Additive progress wire is
  `/os/fetch-progress <slot|patch:name> <queued|fetching>`; `/os/fetched`
  remains terminal. Dist-3 must consume this sequence instead of treating the
  first reply as completion.
- The historical fetch-landing suite's HTTP/diff/prune/Range/file/sim checks
  pass, but its coalescing assertion stops after the two new progress packets.
  The new focused suite verifies both coalesced terminal receipts.
- Older master/point integration harnesses still fail at the documented stale
  setup that discovers no sim device with ID 1. The point helper layer passed
  6/6 before that exception. This matches the 2026-07-13 handoff gotcha.
- Static verification passed: `py_compile` for all touched Python and verifier,
  `bash -n` for touched/adjacent scripts, and `git diff --check`.
- No `.pd` file was edited. Real Pi/LAN transfer, real engine restart and
  audible continuity, and installation hardware remain unverified.
- A cold independent review found and drove fixes for nested-symlink escape,
  stop/restart failure reporting, legacy-link handling, and simulator queue
  serialization; its final re-review was clean.

## Usage at stop

- Codex weekly: 75%, resets `2026-07-20T01:42:13Z`.
- The available Codex meter reports no separate short-window cap.
