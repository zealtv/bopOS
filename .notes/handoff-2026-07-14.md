# Handoff — dashboard d8 chain tied (2026-07-14)

State at handoff: the ratified dashboard seat chain is software-complete and
tied. The durable installation model is now seats-first; discovered devices are
runtime occupants, and simulation is a separate managed mode that fills every
seat without driving real devices. No stitch is claimed. This is the requested
pause before `friction-0-docs`.

## Stitches tied this session

- `d8-1-seat-model`: implementation `268cbd4`, tie `fca36a5`
  - hard-break schema 1 stores durable seats and never persists the device roster
  - seat CRUD, bind/unbind/forget, seat-scoped presets, venue rebind, export, and
    heartbeat assignment replay all use the authoritative seat model
- `d8-2-simulate-toggle`: implementation `5f10f7e`, tie `d5881c3`
  - Dashboard manages `tools/audition.py`, one virtual node per seat
  - sim traffic is loopback-only; teardown restores the performance target and
    real bindings, while the listener puck appears only in simulation
- `d8-3-binding-ux`: implementation `2c79216`, tie `92989bd`
  - empty map clicks author next-free 0-indexed seats; names edit inline and
    element dots drag durable seat positions
  - binding works from seats and devices, reported hostnames seed fresh names,
    swaps preserve seat data, and forget/bulk-forget controls are exposed
  - venue loads identify each rebound seat and each seat waiting for its device

## In-flight / waiting

No in-flight stitch and no dangling `.stitching` directory. Existing hardware
and decision waits remain unchanged. The only loose ends are
`patch-workflow-friction/friction-0-docs` and `friction-1-starter-kit`.

## Recommended next stitch

On Bob's explicit resume, claim `patch-workflow-friction/friction-0-docs` and
continue the friction chain. Do not claim it as part of the d8 autopilot run;
this handoff is the requested pause boundary.

## Gotchas and verification

- `.loom/tied/d8-3-binding-ux/verify_d8_binding_ux.py` passes 10/10 with the
  real dashboard, managed no-audio audition mode, Chromium, and three-node
  simfleet. It caught and fixed a real async picker-reset race during binding.
- The retained d8-1 and d8-2 focused suites pass 9/9 each after d8-3.
- Python compilation, both dashboard JavaScript syntax checks, and
  `git diff --check` pass.
- Pre-seat dashboard state is intentionally ignored rather than migrated.
- Simulation instance count remains uncapped per Bob's explicit deferral.
- Real installation hardware, audible engines, and iPad/touch behavior remain
  unverified. No `.pd` file was edited and bop000 authentication was not needed.

## Usage at stop

- Codex 1-week window: 16% used; resets `2026-07-21 10:40:58 AEST`.
- Three full-reset credits are currently available; none was consumed by this
  d8 run after Bob's reset landed.
