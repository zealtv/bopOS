# Handoff — patch/asset distribution tied (2026-07-14)

State at handoff: the OSC v1.3 patch/asset distribution goal is tied and
software-complete. The tracked authoring roots are now `patches/demo-pd` and
`patches/demo-sc`; `patches/default`, `templates/`, and patch-level
`bopos.config` are gone. Simfleet and audition default to `demo-pd`, the real
dashboard serves both demos, and Chromium proved Send/install/switch for
`demo-sc`. No stitch is claimed. This is the requested pause before `d8-1`.

## Stitches tied this session

- `dist-4-demos`: implementation `83dd6b5`, tie/goal record `9a002a5`
  - renamed the PD demo without changing its `.pd` blob
  - moved and reworded the SC demo; removed the retired config and template root
  - updated active docs, tools, waiting stitch references, and executable
    regression paths to the hard-break names
  - added focused dashboard+simfleet+Chromium evidence under
    `.loom/tied/dist-4-demos/`
- `patch-asset-sync` goal: tie `eae3d7a`
  - all four distribution children are tied
  - `friction-1-starter-kit` is un-waited now that the demos exist

## In-flight / waiting

No in-flight stitch and no dangling `.stitching` directory. Existing hardware
and decision waits remain unchanged. The workable queue is now `d8-1..3`,
`friction-0-docs`, and `friction-1-starter-kit`.

## Recommended next stitch

On Bob's explicit resume, claim `dashboard/d8-1-seat-model`, then work the
ratified seats sequence `d8-1` → `d8-2` → `d8-3`. Pause again before claiming
`friction-0-docs`, as requested. Finishing seats first prevents the composer
guide from documenting the pre-seat dashboard structure.

## Decisions / actions awaiting Bob

No new design decision. `.notes/pd-edits-for-bob.md` now specifies the exact
Bob-owned `demo-pd` asset-context teaching edit. It does not block the tied
distribution goal; `friction-1-starter-kit` will do its software/docs work and
then wait honestly if that PD edit is still outstanding.

## Gotchas and verification

- `.loom/tied/dist-4-demos/verify_dist4_demos.py` passes 16 focused checks
  against the real dashboard, simfleet, and Chromium. The retained dist-3
  dashboard distribution suite passes all 26 checks with `demo-pd` fixtures.
- The launch-context suite passes 68/68 and the preview engine-adapter suite
  passes 73 checks after their executable paths were re-aimed.
- `patches/demo-pd/main.pd` has the exact same Git blob hash as the former
  `patches/default/main.pd`; no `.pd` content was edited.
- `sclang` is not installed on this host, so the moved SC demo was checked via
  manifest, source, launcher, and adapter tests but was not launched or heard.
- Three older retained suites carry already-superseded assumptions (manifest
  fallback, an older PD `netreceive` token, and the retired `gain` selector).
  Relevant path/manifest checks pass; their unrelated stale failures are
  recorded in `.loom/tied/dist-4-demos/results.md`.
- Real Pi transfer, real engine restart/audio continuity, and installation
  hardware remain unverified. bop000 was not needed for this software stitch.

## Usage at stop

- Codex 1-week window: 5% used; resets `2026-07-21T00:40:58Z`
  (`2026-07-21 10:40:58` Melbourne time).
