# Handoff — dashboard Send + Sync tied (2026-07-14)

State at handoff: OSC v1.3 distribution is now complete end to end in software.
The dashboard safely serves repository patches/assets, sends one folder or Sync
all to selected/all-online devices, observes honest fetch phases, tracks the
last acknowledged host fingerprint, lists/switches/drops installed patches,
distinguishes Git-managed targets, and uses the `updatebopos` hard-break naming.
No stitch is claimed. `friction-0-docs` is unblocked; `dist-4-demos` remains the
recommended next stitch so the composer-facing names/layout settle before docs.

## Stitches tied this session

- `dist-3-dashboard-send`: implementation `8afc5c5`, tie `3099295`
  - safe `/patches` + `/assets` manifest/static serving and host metadata
  - per-item Send, Sync all, per-device progress, persisted fingerprint state
  - `/os/patches` dropdown, Git visibility/filtering, pull/drop controls
  - active-patch confirmation in UI and backend; getsamples removed
  - Update bopOS / `updatebopos` throughout technical/facilitator surfaces
  - focused two-device Playwright verifier and screenshot in
    `.loom/tied/dist-3-dashboard-send/`

## In-flight / waiting

No in-flight stitch and no dangling `.stitching` directory. Existing hardware
and decision waits remain unchanged. `friction-0-docs` is now a loose end because
dist-2 and dist-3 are tied; `friction-1-starter-kit` still waits for dist-4.

## Recommended next stitch

Claim `dist-4-demos`: rename `default` to `demo-pd`, move the SC template to
`patches/demo-sc`, remove retired demo machinery, validate both manifests, and
record (without editing `.pd`) any asset-context PD work for Bob. After dist-4,
the composer docs/starter-kit work can use final demo names; the dashboard seats
sequence remains `d8-1` then `d8-2`, then `d8-3`.

## Decisions awaiting Bob

None.

## Gotchas and verification

- `.loom/tied/dist-3-dashboard-send/verify_dist3_dashboard_send.py` passes every
  check with the real dashboard, two simulated devices, and Playwright. It covers
  safe manifests/static serving, hidden/symlink refusal, metadata, dropdowns,
  queued/fetching/terminal state, stale/re-send, active confirmation, exact Sync
  all iteration, mixed Git/mirrored all-target behavior, drops, and updatebopos.
- Dist-2 focused verification and ui-0/ui-1/ui-3 regressions pass. Python/JS
  static checks and `git diff --check` pass.
- OSC v1.3 receipts have no request ID. The dashboard therefore permits only one
  fetch generation per node/slot. If its terminal receipt is lost for 30 minutes,
  an explicit timeout tombstone blocks retry until dashboard restart; this avoids
  a late old receipt falsely certifying newer host bytes.
- The old fetch-landing suite retains its documented stale coalescing assertion
  (it stops at progress packets); its other checks pass. The old role-removal
  browser verifier expects retired `gain` selectors; its manifest checks pass.
- Real Pi/LAN transfer, real engine restart/audible continuity, iPad/touch, and
  installation hardware remain unverified. No `.pd` file was edited.

## Usage at stop

- Codex weekly: 90% used (10% remaining), resets `2026-07-20T01:42:13Z`.
