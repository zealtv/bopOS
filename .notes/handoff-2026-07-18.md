# Handoff — 2026-07-18 autopilot session (Show tab first slice)

## State of play

Bob's 2026-07-18 braindump for the "sequencer tab first slice" is now laid
out as thread `14-show-tab` (compared against the 2026-07-15 brainstorm in
the goal stitch; braindump kept verbatim there), and **stitches 1–5 of 8 are
tied**: the design note, the show document model + persistence + WS surface,
the playback engine, the Show tab UI (Sequencer tab renamed), and the
context-sensitive inspector with the OSC message builder. The dashboard can
now author a show (steps/sections/messages), edit step properties and build
param/cue/point/raw messages through pickers, and *play* it — durations,
play-n-times, the full then-action vocabulary (any/other with correct
shuffle-bag semantics, goto, section walks), pause/resume, stop-all, and
forward-synced cues on the shared clock — all verified against simfleet.

## Tied this session (commit hashes)

- `95f5f61` thread layout + CLAUDE.md ordering update
- `3207751` stitch 1 — `.notes/show-tab-design-2026-07-18.md` (authoritative
  design: schema, WS API, playback semantics; read it before touching 6–8)
- `93f99e9` stitch 2 — `dashboard/show_model.py`, WS CRUD, `current_show`
- `f792174` stitch 3 — `dashboard/show_engine.py`, transport WS,
  `show_playback`, simfleet cue-recv type logging
- `22e0fb9` stitch 4 — Show tab UI (`static/js/show.js`, tab rename)
- `9d39c85` stitch 5 — inspector + message builder

Every stitch's `verify_*.py` + worklog travel with it in `.loom/tied/`.
Run any of them with `~/.venvs/bopos/bin/python <script>` from anywhere
(repo located by marker).

## Remaining loose ends (all unclaimed, in order)

- `14-show-tab/6-message-editing` — copy/cut/paste/move/delete message
  pills; step/divider add/move/delete. Frontend + Playwright verify only;
  the backend ops (add_message-as-paste, move_message, move_item,
  remove_item) already exist and are verified.
- `14-show-tab/7-osc-consoles` — outgoing/incoming consoles. Needs small
  backend taps (`osc_out`/`osc_in` broadcasts per design note §3 end) +
  frontend + verify.
- `14-show-tab/8-docs-and-handoff` — docs, CLAUDE.md close-out, final
  `grep -ri sequencer` sweep.
- Then resume `patch-workflow-friction/friction-0..1` per CLAUDE.md.

## Why the session stopped

Claude 5-hour session cap hit 78% (threshold 85) — resets **06:40 UTC
2026-07-18**; Claude weeklies are low (Fable 21%, all-models 22%, reset
07-20 09:00 UTC), so the next session has full room. Codex weekly was at
**76% used** mid-session; Bob's standing rule from this session: **keep
≥20% codex weekly in reserve** (i.e. stop delegating to codex at 80%
used). Meter it from the newest `~/.codex/sessions/<Y/M/D>/rollout-*.jsonl`
→ `"rate_limits"` → `primary.used_percent` (10080-min window). Stitches 6–8
are comfortably Claude-only next session (Sonnet delegates or inline).

## Gotchas discovered (healed where noted)

- Sonnet delegates on meaty backend stitches cost ~30 session-cap points
  each; codex-implement stitches cost ~4–5 (orchestrator review only).
  Balance against the codex reserve rule above.
- `codex exec resume` rejects `-s/-C/-o` (inherits session config) — healed
  into the canonical codex-implement skill Learnings, along with the codex
  metering method.
- Codex sandbox can't open sockets: it writes the Playwright verify, the
  orchestrator runs it (already a known learning; held true twice more).
- `loom.sh tie` refuses while a `__pycache__` dir sits in the stitch —
  `rm -rf` it first.
- Stitch-3 delegate fixed a latent wait-loop bug in the *tied* stitch-2
  verify (proper-subset condition) — both verifies re-run green.
- No run-context seed reaches the dashboard process today; `ShowEngine`
  reads `BOPOS_SEED` if present, OS randomness otherwise. Flagged in the
  stitch-3 worklog as a candidate future stitch (not blocking).

## For the next session

Continue from this handoff: claim `6-message-editing`, then `7-osc-consoles`,
then `8-docs-and-handoff` (each has complete instructions; the design note
is authoritative). Before starting, re-run the two UI verifies in
`.loom/tied/4-tab-ui/` and `.loom/tied/5-inspector/` to confirm a clean
base. Gremlin-ping Bob when the thread ties.
