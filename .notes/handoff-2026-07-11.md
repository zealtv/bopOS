# Handoff — 2026-07-11 (autopilot session)

## State of play

Contract v1.1's §4.1 provided terms are now **implemented, not just
ratified**: the dashboard broadcasts `/all/os/master` (raw mixes only on the
wire — the VCA multiply, `volume_param`, and `resend_volumes` are deleted)
and `/pt` point geometry (one atomic frame, ~25 Hz only while moving,
silence = hold, sparse + `/pt/clear` authoring forms). Every node decomposes
points locally via the new shared module `python/pointfield.py` — the real
helper and simfleet import the same code, and the engine receives only
shaped scalars (`/pt <pointId> <element> <v>` on 6661, element 1-based by
assignment pair order, release-to-zero on removal). `/os/assign` carries
true-N element pairs node-side. The hardware onboarding doc Bob asked for
in the seam-1 review exists (`docs/HARDWARE.md`). `spatial-1` is un-waited
(its blocker was seam-3 tying).

## Tied this session

- `patch-seam/seam-2-master-term` — a83e5c2 (tie: 9fad647). Verify:
  9/9 browser-free checks (sniffer socket on the shared sim command port).
- `patch-seam/seam-3-points-node-side` — fdbacbb (tie: d187701). Verify:
  18/18 — real helper.py driven in-process (sync-2 style) + full
  dashboard/simfleet stack; every sim-logged proximity equals
  `falloff(distance)` recomputed from sniffed frames.
- `hw-onboarding-doc` — 1adb69d (tie: e97c2df). Docs-only; board table
  ships with honest blank "verified" cells.

## Recommended next stitch

**`patch-seam/seam-4-facilitator-promotion`** (then `seam-5`, numeric
order). Deliberately not started at 70% session cap: it's a Playwright
stitch (facilitator.html param promotion + `installation.json` command
allowlist + browser verify) and the venv has **no Playwright/Chromium yet**
— install per CLAUDE.md before starting. Copy the newest tied dashboard
verify as the template.

After the seam thread: `spatial-1-authoring-ui` (now a loose end, gate open
— it drives the `set_points`/`set_point`/`clear_point` ws surface and
`dashboard/points.py`; Bob's element-dot direction applies) and
`spatial-2-synced-start`.

## Waiting / decisions with Bob

- `pd-edits-for-bob.waiting` — A5 (master → `bopos.out~`) and A6
  (`bopos.point` receiver) now carry the final live spellings, updated this
  session. Hardware/audible verification of both terms needs these PD edits
  plus a rig.
- Element index base: I ruled **1-based** on the engine wire (contract pins
  pair order, not base; Bob says "element 1"). Recorded in seam-3 results
  and pd-edits A6 — flag to Bob, cheap to flip before PD receivers exist.
- Unchanged: `sync-4` (hardware run), `dashboard-6-rig-adoption`,
  `audition-0` macOS spike, `audio-input/*`, `scene-sequencing` (paused),
  `zero-1/2`.

## Gotchas (healed where marked)

- **The `~/.venvs/bopos` venv did not exist on this machine** — created per
  the CLAUDE.md recipe (requirements.txt covers websockets; pyOSC3 added).
  Playwright/Chromium still absent (seam-4 needs them).
- OSC floats are 32-bit: verify assertions must compare wire floats with a
  tolerance (0.6 arrives as 0.6000000238…). Both new verifies encode this.
- Broadcast + SO_REUSEPORT on Linux delivers to **every** binder (re-proved
  with a 3-binder spike): the sniffer-socket pattern in the two new verifies
  depends on it, with the dashboard's `--osc-target 255.255.255.255`.
- CLAUDE.md thread-ordering section updated to post-seam-3 reality (healed);
  stale "await implementation" phrasing removed.
- `facilitator.js` had a comment pointing at the deleted Python
  `volume_param` (healed — the JS mirror itself stays, it picks the volume
  card's param).

## Usage at stop (2026-07-11, Fable orchestrator)

- 5-hour session: **~70%** ← binding; resets 2026-07-11T05:30Z
- weekly (Fable): 24%; resets 2026-07-13T09:00Z
- weekly (all models): 18%; resets 2026-07-13T09:00Z

Next session can start after the session-cap reset (hours, not days).
Tell it: continue from this handoff; next is seam-4 (install Playwright
into `~/.venvs/bopos` first).
